[user]
	name = xmatheus
	email = matheuscorreia559@gmail.com
[core]
	editor = code
[alias]
    s = !git status -s
	c = !git add --all && git commit -m
	l = !git log --reverse --pretty=format:'%C(yellow)%h %C(red)%d %C(white)%s - %C(cyan)%cn, %C(green)%cr'
[pager]
	log = false
[push]
	followTags = true
[credential]
	helper = store
